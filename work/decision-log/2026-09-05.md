# Decision Log — 2026-09-05

## 1. Wrote up the maintenance handoff doc with six flagged gaps

**Working on:** task described as "the handoff doc is updated with all six
missing pieces spelled out — Mermaid skill, artifact logging, curation gate
enforcement, frontmatter traceability, retire flagging, see session in
Claude for maintenance — using ideas from this [Daniel Miessler's
ULWork/LifeOS/Fabric/TELOS/Substrate write-up] to update the site."

**What I found:** no "handoff doc" existed anywhere in the repo, no related
GitHub issues/PRs, and this branch had zero commits ahead of `main` — the
task description was the only artifact to work from. Confirmed scope with
the user before writing into `CLAUDE.md` (the canonical source of truth)
rather than guessing: (a) create `docs/maintenance-handoff.md` *and* add a
pointer from `CLAUDE.md`, and (b) my interpretation of the six pieces,
grounded in this repo's actual mechanics rather than abstract labels.

**What changed:**
- `docs/maintenance-handoff.md` — new. Documents the existing system
  (decision log, decision-digest agent, `lint-content.mjs` R1-R30, the
  ASVS/AISVS translator→reviewer→QA-gate→build pipeline in `scripts/asvs/`)
  as the baseline, then spells out the six gaps against that baseline:
  1. **Mermaid skill** — added a `mermaid` flowchart of the ASVS pipeline
     (previously only described in prose across three files).
  2. **Artifact logging** — new convention: any decision-log entry
     involving a published Claude Artifact must record its URL and what it
     shows (or explicitly note none was published, as this entry does).
  3. **Curation gate enforcement** — named the real gap: the
     `Translation Status: ✅ Complete` marker and the two-stage QA PASS
     requirement in `scripts/asvs/README.md` are conventions, not checked
     by `build-review-posts.py` or `lint-content.mjs`. Spelled out the fix
     (build script refuses ungated fork files; new lint rule after R30)
     without implementing it yet.
  4. **Frontmatter traceability** — named that source-PR provenance
     (`OWASP ASVS PR #3254`) lives only in post body prose, not frontmatter,
     unlike Miessler's pattern of writing tracking refs into YAML. Proposed
     `source_pr` / `qa_status` frontmatter fields, auto-set by
     `build-review-posts.py` from data the fork file already carries.
  5. **Retire flagging** — CLAUDE.md's *Retired Projects* table covers
     retired sibling codebases but nothing covers retired *content*.
     Proposed `retired` / `retired_date` / `retired_reason` frontmatter,
     parallel to how `hidden: true` already works in `getPublicPosts()`.
  6. **Session continuity pointer** — added this session's URL
     (`018AXSV6NqHZuiaMd5Edptgc`) to the top of the handoff doc as the
     place to check intent when the doc and the code seem to disagree.
  - §3 maps Miessler's ULWork/LifeOS/Fabric/TELOS/Substrate pieces onto
    what this repo already has, and says explicitly why the ones that
    *don't* map (a separate `TASKLIST.md` blackboard, a Substrate-style
    knowledge graph) aren't being adopted at this site's scale — avoids
    silently importing infrastructure this project doesn't need per its own
    lean-deps/no-parallel-data-formats rules.
- `CLAUDE.md` — added a one-paragraph pointer to the new doc inside
  *Decision Capture & Daily Digest*, keeping CLAUDE.md itself as the single
  index rather than duplicating the handoff doc's content into it.

**Alternative considered:** fold all six pieces directly into `CLAUDE.md`
with no separate doc. Skipped per the user's explicit choice ("both") —
`CLAUDE.md` stays a canonical-rules index; the handoff doc carries the
narrative detail (diagram, before/after examples, Miessler mapping) that
would otherwise bloat CLAUDE.md.

**Alternative considered:** implement §2.3–§2.5 (the enforcement code, the
new frontmatter fields, the lint rule) in this same session. Skipped — the
task as given ("handoff doc ... with all six missing pieces spelled out")
is a specification task, not an implementation task; §4 of the new doc
gives the next session a concrete, ordered build plan instead of a partial,
untested implementation of a lint rule and a Python script change with no
existing test coverage to validate against.

**Verification:**
- `grep -rn "handoff" docs/ CLAUDE.md` — one hit, the new doc's own
  self-reference; no dangling old references to reconcile.
- Confirmed `docs/asvs-review-pages-plan.md`, referenced by `CLAUDE.md`'s
  *Current Page Count* section, does not actually exist in the repo — noted
  here rather than silently fixed, since it's outside this task's scope;
  flagging so a future session doesn't waste time assuming it's there.
- No code paths touched (`src/`, `scripts/`) — `tsc --noEmit` / `npm run
  build` unaffected by this change; not re-run since only Markdown changed.

**Artifact published:** none this session — output was repo files only
(`docs/maintenance-handoff.md`, the `CLAUDE.md` pointer, this log entry).

## Do not retry

- Guessing at the meaning of "the six missing pieces" without checking for
  an existing handoff doc or GitHub issue first — none existed; the task
  description's phrasing implied prior context that turned out not to be
  in this repo, this branch, or GitHub.

## Follow-ups

- Implement §2.3 (curation gate enforcement) and §2.4 (frontmatter
  traceability) together, per `docs/maintenance-handoff.md` §4 — traceability
  fields first, gate check second, since the gate depends on the fields
  existing.
- Implement §2.5 (retire flagging) whenever the first post/article actually
  needs retiring — not urgent, nothing is stale yet.
- Separately: `docs/asvs-review-pages-plan.md` is referenced by `CLAUDE.md`
  but missing from the repo. Worth a future session either writing it or
  removing the reference.

## 2. Task 1 of the real handoff doc: repo maintenance scan

**Working on:** the user supplied the actual, complete handoff doc this
session was missing this morning (a 6-task "Content Maintenance & Visual
Automation" plan sourced from Notion). Executed Task 1 (repo scan —
standalone, first in the suggested build order) in full; Tasks 2–6 are
Notion-integrated and/or design decisions, left for follow-up sessions per
the doc's own suggested order.

**What changed:**
- `npm install` (no `node_modules` existed) + `npm run build` (no `.next`
  existed) to get real static-page output as ground truth, rather than
  trusting prose descriptions of routes.
- **Fixed:** 2 dead internal links in
  `posts/github-supply-chain-pipeline-perimeter.md` (`/posts/...` →
  `/blog/...` — wrong route prefix, verified against real build output).
- **Fixed:** the "stray double-quote rendering bug on tech-support posts"
  the handoff named as a known open issue. Root cause: `@tailwindcss/typography`'s
  `.prose` blockquote styling adds decorative curly quote marks via CSS
  `content: open-quote`/`close-quote` on every blockquote's first/last
  paragraph — this site's blockquotes are all callouts, never literal
  quotations, and several (this post's translation-notice banner included)
  already contain their own literal `"..."` text, so readers saw four quote
  marks (two mismatched styles) around one callout. Fixed with Tailwind's
  own documented opt-out (`content: none` on those pseudo-elements) in
  `src/app/globals.css` — no markdown touched. Confirmed via Playwright
  screenshots (pre-installed Chromium) before/after, and via computed-style
  inspection (`content` → `none` post-fix). Also confirmed the same
  literal-quote-in-blockquote pattern exists in 52 files total, not just
  tech-support — same underlying CSS bug, wider blast radius than the
  handoff doc's framing suggested.
- **Found, root-caused, did not fix (content decision, not a scan fix):**
  the ASVS review hub's "twenty-seven bilingual documents" / "(27
  documents)" text is stale against its own table, which lists 28 rows —
  and the homepage's "Latest insights" structurally cannot reflect the
  Aug 22–27 ASVS/AISVS launch because all 48 of those posts are
  (correctly, by design) `hidden: true`. The real fix for "front page
  doesn't reflect newer content" is a new public announcement post, not a
  hidden-post-counting change.
- Wrote `docs/maintenance-scan-2026-09-05.md` — the full report (internal
  links, external links, the quote bug, the stat mismatch, 8 stale-post
  candidates + all 6 research articles, plus incidental findings: `npm
  audit` now shows 5 high-severity vulns against `CLAUDE.md`'s stale
  "0 vulnerabilities" claim, and `docs/asvs-review-pages-plan.md` is
  referenced but missing).

**Alternative considered:** doing a full HTTP status check on all 620
unique external URLs found across the content. **Skipped, not silently** —
direct `curl` to any non-`github.com` host returned `403`/`EGRESS_BLOCKED`
from this session's network policy (tested against 6 different hosts,
including `owasp.org` and `linkedin.com`, both explicitly named as "key
URLs" in `CLAUDE.md`'s own Link Validation section); the `WebFetch` tool
hit the identical block. Per this environment's own proxy guidance, a
blocked host is reported, not retried or routed around. Checked the 4
highest-value `github.com` links by hand instead (the two translation PRs,
the working fork, the named `mcp-sentinel-scanner` repo) since GitHub is
this session's one allowed exception, and documented the constraint plainly
in the report rather than claiming a full check that didn't happen.

**Do not retry:** direct HTTP/HTTPS access (`curl`, `WebFetch`) to any host
other than `github.com` from this session — confirmed blocked by egress
policy, not a transient failure. A full external-link audit needs a
different environment (CI runner, local machine, or a session with broader
egress).

**Verification:** `npx tsc --noEmit` clean; `npm run lint:content` — 0
errors, 8 pre-existing R6 notices (unchanged baseline); `npm run lint:code`
— 0 errors; `npm run build` — 81 pages both before and after the fixes;
internal-link checker — 2 broken → 0 broken; `<h1>` count = 1 spot-checked
on 3 pages post-fix.

**Artifact published:** none — output was repo files
(`docs/maintenance-scan-2026-09-05.md`, the two content/CSS fixes, this log
entry). Screenshots used for visual verification of the quote-bug fix were
local temp files, not published anywhere.

## Follow-ups (Task 1 scan)

- Task 2 (Mermaid-generation skill), Task 5 (frontmatter ↔ Notion
  traceability field), Task 3 (Visual Artifacts logging), Task 6 (stale
  flagging automation/cadence), Task 4 (Notion curation gate) — not started
  this session; see the handoff doc's suggested build order.
- Re-run the external-link check (§2 of the scan report) from an
  environment that can reach the open internet — 620 URLs across 93 hosts
  were extracted but not checked.
- Decide and fix the ASVS hub's "27" vs. actual-28-documents count (§4a of
  the scan report) — needs a human call on whether the standalone glossary
  page should count separately from Appendix A's glossary.
- Write a new public (non-hidden) announcement post for the completed
  ASVS/AISVS translation launch if the homepage should reflect it (§4b) —
  an editorial decision, not something this scan should do unilaterally.
- `npm audit` now shows 5 high-severity vulnerabilities (transitively via
  `sharp`) — `CLAUDE.md`'s "0 vulnerabilities" claim needs re-verification
  and either remediation or an updated Known Accepted Risks entry.
- The 8 stale-post candidates and 6 stale research articles (§5) are raw
  input for Task 6's stale-flagging automation, not yet acted on.
