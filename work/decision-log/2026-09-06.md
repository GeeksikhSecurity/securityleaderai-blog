# Decision Log — 2026-09-06

## 1. Finished Tasks 2, 5, 6 of the Content Maintenance & Visual Automation handoff; specified (not executed) Tasks 3 and 4

**Working on:** user instruction "finish all tasks and use judgement. Approve
design functions" — continuing `docs/content-maintenance-visual-automation-handoff.md`
from yesterday's Task 1 into the remaining five tasks, per its suggested
build order (5 → 3 → 6 → 4, skipping ahead where a task turned out blocked).

**What I found before starting:** this session's Notion MCP connection is
disconnected (confirmed via the tool-availability system reminder, not
assumed) — Tasks 3 and 4 are Notion-native (a database row write, a Notion
view/schema decision) and cannot be executed live from here. Rather than
skip them silently or fake a Notion write, I built the repo-side half of
each and specified the Notion-side half precisely enough for a
Notion-connected session to finish without re-deriving the design.

**What changed:**

- **Task 2 (Mermaid-generation skill) — done.**
  `.claude/skills/mermaid-diagram/SKILL.md` + `scripts/mermaid/render.mjs`.
  Decided: styling via `themeVariables`+`themeCSS` baked into the render
  step (not a post-render SVG pass) so every diagram is reproducible from
  its `.mmd` source alone; path convention `content/diagrams/<slug>.mmd` →
  `public/images/diagrams/<slug>.svg`; zero new npm dependencies — the
  mermaid UMD bundle is fetched to a local cache (not installed) and
  rendered via `playwright`, resolved from a global toolchain location
  outside `package.json` (same pattern as `scripts/asvs/*.py` being
  external Python tooling). Built and verified a real worked example:
  `content/diagrams/tech-support-scam-elders.mmd`, embedded in that post,
  confirmed rendering correctly on the live page via Playwright screenshot.
  Caught and fixed a real bug in the render script itself: Mermaid's
  syntax-error failure mode is a *successfully rendered* placeholder image
  ("Syntax error in text"), not a thrown error — the render script now
  detects that string and fails loud instead of silently writing a broken
  diagram. Found this by actually triggering it (an escaped `\"` inside a
  quoted node label), not by inspection.
- **Task 5 (frontmatter ↔ Notion traceability) — done.** `notion_url`
  frontmatter field, `Post.notionUrl` in `src/lib/posts.ts`, auto-stamped
  by `scripts/mermaid/render.mjs --post <file> --notion-url <url>` in the
  same step a diagram renders (per the task's own guess that this was
  "likely" the right pipeline point). `--post`/`--notion-url` must be
  given together — fails loud on a mismatched pair rather than a silent
  partial stamp.
- **Task 6 (stale/retire flagging) — done.** `scripts/check-stale-content.mjs`
  (`npm run check:stale`) reuses yesterday's Task 1 git-log staleness
  method exactly (no second method invented) plus a research.ts-specific
  check against each article's hardcoded date. Surface: a dated report
  under `outputs/` (same convention as the decision-digest agent's daily
  summaries) plus `.github/workflows/monthly-content-review.yml` — cron on
  the 1st of each month, commits the report, files a
  `Monthly Content Review — YYYY-MM` GitHub issue. Chose a GitHub issue
  over a Notion view specifically because it needs no live Notion
  connection. Also built `retired` / `retired_date` / `retired_reason`
  frontmatter + lint rule **R31** (mirrors R9: an unexplained flag is as
  bad as no flag) + `getPublicPosts()`/`getPublicLocalePosts()` exclusion —
  without this, the stale-candidate list from `check:stale` would have
  nothing to feed into.
- **Task 3 (Visual Artifacts logging) — staged, not live.** Every
  successful diagram render appends a record (artifact type, synthesis
  date, file paths, source post, Notion URL, `notionSyncStatus: "pending"`)
  to `docs/visual-artifacts-log.jsonl`, shaped exactly like the intended
  Notion row. The actual Notion row creation and the Research
  Items/Knowledge Graph relation are left for a Notion-connected session —
  documented as a queue to drain, not a finished sync.
- **Task 4 (curation gate) — decided, not implemented.** Recommended and
  documented a **soft gate** (a filtered Notion view surfacing
  unlinked items) over a hard gate: a hard gate blocks the workflow's own
  "quick idea before I forget it" capture stage from the moment of entry,
  which fights the thing Stage 1 (Capture) exists to support; soft gets
  the same visibility without that friction, and is cheap to tighten later
  if unlinked items pile up unaddressed. No Notion schema/view was created
  — no live connection to create it with.
- Updated `docs/content-maintenance-visual-automation-handoff.md`'s
  per-task status sections to reflect all of the above, and `CLAUDE.md`
  with the two new optional frontmatter conventions (`notion_url`,
  `retired`/`retired_date`/`retired_reason`) and the diagram-embedding
  convention.

**Alternative considered:** silently skipping Tasks 3 and 4 since Notion
isn't reachable. Rejected — per CLAUDE.md's "fail fast, fail loud" and
"verify before operating," an unreachable dependency gets reported and
worked around at the repo-side half, not quietly dropped from the task
list.

**Alternative considered:** adding `mermaid` and a headless-browser package
(e.g. `@mermaid-js/mermaid-cli`, which bundles its own puppeteer) as project
`devDependencies` to render diagrams. Rejected per CLAUDE.md's lean-deps
policy ("zero new npm dependencies... check dependency count before
adding") — `mermaid-cli`'s puppeteer alone pulls in a full Chromium
download and a large dependency tree for something that only ever runs at
content-authoring time, never in the app's build or runtime. Fetching the
mermaid UMD bundle directly and reusing the machine's own Playwright/
Chromium toolchain (same one used for this session's own visual
verification work) gets an identical rendering result with a net dependency
change of zero in `package.json`.

**Do not retry:** `NODE_PATH` for resolving `playwright` from Node's ESM
`import()` — Node's ESM loader does not honor `NODE_PATH` (only CommonJS
`require` does). `scripts/mermaid/render.mjs` uses `createRequire` +
`require.resolve(..., { paths: [...] })` instead, which works regardless of
environment variables being set correctly.

**Verification:**
- `npx tsc --noEmit` — clean.
- `npm run lint:content` — 0 errors, 8 pre-existing notices (unchanged).
- `npm run lint:code` — 0 errors.
- `npm run build` — succeeds, page count unchanged apart from the one new
  diagram image (not a page).
- R31 self-tested both directions on scratch copies (never committed):
  `retired: true` alone → 2 errors (missing reason, missing date); reason/
  date alone without `retired: true` → 1 error; all three together → clean.
- `rm -rf node_modules && npm ci` — clean install, confirms the `package.json`
  script addition didn't touch the dependency graph.
- Ran `vercel-security-monitor.yml`'s own action-pin-integrity grep locally
  against the new `monthly-content-review.yml` — passes (`actions/
  github-script@v7` is tag-pinned, which that check explicitly allows for
  `actions/*` publishers).
- `content-rigor.yml` and `code-quality.yml`'s exact commands (`lint:content`,
  `lint:code`, `lint` (tsc), `build`) all run clean locally, standing in for
  actually watching the CI run (this branch hadn't been pushed with these
  changes yet at verification time).
- Mermaid render script's fail-loud path (§ Task 2) verified live: a
  deliberately broken `.mmd` (escaped quote) makes the script exit 1 and
  write no file; the corrected version renders and passes.

**Artifact published:** none — output was repo files. The rendered
diagram (`public/images/diagrams/tech-support-scam-elders.svg`) is
committed to the repo, not published as a Claude Artifact.

## Follow-ups

- From a Notion-connected session: drain `docs/visual-artifacts-log.jsonl`
  into real Visual Artifacts rows (Task 3), and create the Research
  Items/Knowledge Graph relation once that database exists.
- From a Notion-connected session: build the Task 4 soft-gate view per the
  spec in the handoff doc.
- Generate diagrams for other structurally-diagrammable posts using the
  new skill — only one worked example exists so far.
- Everything carried over from 2026-09-05's follow-ups not yet acted on:
  the ASVS hub's stale "27 documents" count, a possible new public
  announcement post for the ASVS/AISVS launch, the `npm audit` finding
  (5 high-severity vulns), the missing `docs/asvs-review-pages-plan.md`,
  and re-running the external-link check from an environment with normal
  egress.
